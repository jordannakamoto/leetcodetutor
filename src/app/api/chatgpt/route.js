import { NextRequest } from "next/server";

export async function GET(req){
    data = req.data | {};

    // build a context from the problem statement and its solutions:
    // generate test cases
    // generate a hint on the next step to solve the problem without giving away the answer
} 